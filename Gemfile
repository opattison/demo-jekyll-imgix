source 'https://rubygems.org'

gem 'jekyll', '~> 3.1'

group :jekyll_plugins do
  gem 'jekyll-imgix'
end


# the following gems are not needed for jekyll or imgix directly
gem 'rake' # only used for convenient serve/deploy commands
gem 's3_website' # only used for deploying
